{
  "name": "telegram-worker-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for Telegram/WhatsApp API",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
